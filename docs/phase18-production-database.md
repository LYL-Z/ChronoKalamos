# Phase 18 生产数据库记录

## 已应用迁移

- `20260801144734_phase18_authoritative_world_systems.sql`
- `20260801151512_phase18_reduce_system_ledger_grants.sql`

应用前已在生产连接中执行完整 `BEGIN / ROLLBACK` 演练。演练后确认新表和 RPC 均不存在，再通过本地迁移账本正式应用。远程迁移与本地文件名现为 32 条一致记录。

## 数据结果

- 原有 `game_sessions`：12 行，应用前后不变。
- 原有 `game_checkpoints`：13 行，应用前后不变。
- 原有 `game_turns`：1 行，应用前后不变。
- 存档和检查点缺少 `systems`：0 行。
- 验收后的 `game_system_actions`：0 行。测试用户删除后已级联清理。

## 权限结果

- `authenticated`：仅可 `SELECT` 自有账本，并受已登记 MFA 的限制性策略约束。
- `anon`：没有表级授权。
- `service_role`：仅可 `SELECT` 账本，并可执行写入 RPC。
- 私有默认状态和验证函数：仅函数所有者可执行。
- 写入 RPC：仅函数所有者和 `service_role` 可执行。

## 真实隔离验收

使用两个临时匿名身份运行。第一身份完成 18 步链。第二身份查询第一身份的会话账本得到 0 行。第一身份直接插入账本被拒绝。第一身份直接执行服务端写 RPC也被拒绝。重复提交婚约动作返回原账本 ID、原状态版本和 `duplicate: true`。

测试结束后两个临时身份均通过管理员 API 精确删除。没有批量清理或宽范围删除。

## 仍存在的控制台告警

Supabase Advisor 仍报告：

1. 匿名登录可访问自有数据的策略。它是当前游客模式的预期设计，不等于跨用户开放。
2. 泄露密码保护未开启。该项属于 Auth 控制台配置，未由数据库迁移解决。
3. 六项未使用索引提示。当前样本量很小，不能据此删除来源、地图或回合索引。

阶段完成不等于历史内容认证，也不等于第二场景可上线。
