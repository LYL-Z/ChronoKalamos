执行 ChronoKalamos 阶段10，实现受限生成的事件与后果系统。

建立EventTemplate、RuleExpression、Consequence、NarrativeBeat和ReplaySummary。事件、状态变化、关系变化、金钱、风险、时间推进和章节结局必须由编辑模板与规则决定。

DeepSeek只能返回叙事文本、已允许的选项文案、证据引用和与模板一致的表达变体。模型不得创建新地点、新人物、新物品或任意状态字段。验证失败最多重试一次，仍失败则本回合不提交。

加入关系记忆、风险时钟、章节结局、回顾摘要和重玩入口。建立至少90个评测案例，覆盖提示注入、时代错置、非法状态、重复请求、超时、无来源声明和三个出身。
