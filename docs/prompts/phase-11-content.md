执行 ChronoKalamos 阶段11，只建设742年长安精品内容包。

先完整阅读 `docs/phase11-content-production-bible.md`、`docs/narrative-contract.md`、`docs/consequence-system.md`、`docs/schemas/phase11-production-bundle.schema.json` 和 `content/tang-changan-742/production-batch-plan.json`。任何新增机制都必须先判断属于权威状态、派生读模型还是表现提示。表现层不得写入状态。

围绕三个现有出身制作三条短章节线。每条线至少包含：开场事件、五个核心事件、两个后果事件、一个章节收束、四名以上有明确功能的NPC。

所有事实性内容必须有HistoricalSource和HistoricalClaim编号。每条内容必须标注史料记载、合理重建或叙事虚构。不得复制CHGIS或CBDB受限数据。不得把AI生成连接句标成史料。

未经外部历史学者审阅的内容只能标 `provisional`。它可以进入带永久醒目提示的 `public-beta-unreviewed` 通道，但不得升级为 `reviewed` 或 `published`，不得宣称获得学者或机构认证。

电影化表现属于渲染层，不得改变事件、状态或证据强度。为每个候选事件提供一个镜头合同和一条人物台词。台词一律标叙事虚构，字幕必须常显，声音默认关闭。设备合成音只能写成现代普通话节奏预演，不得声称是唐代语音复原，不得克隆真人声纹。

引用目标按“事件—来源链接”计算。每个链接必须标明它只支持事件背景、声明还是场景地点。目标约100条链接不等于100部独立文献；独立书目数量必须另行报告，禁止用重复引用伪装研究覆盖。

三维社会反馈只作为现有 `WorldState` 的确定性投影。每个选择必须分别声明身份准入、社会声望和阵营立场的影响；没有因果依据时写 `no_change`，不得为制造“每次都变化”的感觉编造后果。

为8个既定证据地点建立表层、隐藏和关联线索。朱雀大街、曲江和安仁坊不在当前地点合同中；除非先完成ADR、来源和地图迁移，不得静默加入或替换。

为每个事件提供三幕节拍、具象后果、低动态替代、字幕、音频状态和至少三条证据链接。目标评测矩阵为240项。现有93项阶段10评测只作为回归基线，不得冒充阶段11新增机制已验证。
