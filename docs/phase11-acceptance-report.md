# 阶段11公开测试版验收报告

验收日期：2026-07-29
内容版本：11.0.0
发布通道：`public-beta-unreviewed`
历史审阅：`pending`
历史学家认证声明：`false`

## 结论

阶段11的工程与内容生产门禁已经通过。27个事件可进入公开测试运行时。新增历史内容仍为 `provisional`。公开可玩不等于史实获批，也不构成外部学者或机构背书。

外部历史学家认证暂缓。审阅者列表保持为空。网站必须持续展示“未经外部历史学家认证”的提示。后续只有取得可核审阅证据后，才能把具体条目升级为 `reviewed` 或 `published`。

## 交付状态

| 项目 | 结果 | 证据 |
|---|---|---|
| 三条出身线 | 通过 | 每条9个事件：1开场、5核心、2后果、1收束 |
| 事件与选择 | 通过 | 27个事件、81个受限选择 |
| 命名NPC | 通过 | 12名；全部标为叙事虚构 |
| 地点 | 通过 | 8个证据地点；阶段11新增地点保持 `provisional` |
| 声明 | 通过 | 40条；阶段11新增27条保持 `provisional` |
| 来源与引用 | 通过 | 18条来源台账；100条事件—来源链接；CHGIS与CBDB零复制 |
| 人物台词 | 通过 | 27条中文台词；全部为叙事虚构并配字幕 |
| 电影化场景 | 通过 | 27个表现合同；镜头、声音和动效不写游戏状态 |
| 量产规范 | 通过 | 玩法、叙事、美术、音频和跨引擎合同有机器校验 |
| Phase 11评测矩阵 | 通过 | 240项新增评测通过 |
| 事件编译 | 通过 | 27个事件可编译为现有 `EventTemplate` |
| 版本兼容 | 通过 | 新会话使用11.0.0；已推进的10.0.0会话继续使用旧目录 |
| 外部审阅 | 待定 | `pending`；审阅者列表为空；不宣称认证 |

## Supabase

已应用以下阶段11迁移：

- `20260728141153_phase11_voice_line_candidates`
- `20260729062713_phase11_public_beta_runtime`

服务端候选快照共132条。匿名客户端不能读取候选审阅表。公开运行时目录包含27个阶段11事件。游戏会话记录 `content_version`，避免旧存档静默换目录。

真实隔离测试 `npm run test:supabase:live` 为5/5通过。它覆盖候选表隔离、公开目录读取、跨用户存档、游客升级、服务端提交和准入控制。

Supabase Advisor仍有两类已知提示：

- 游客产品路径允许匿名身份访问本人数据。该项由RLS和所有权条件约束，不是开放跨用户读取。
- 泄露密码保护尚未开启。该项需要在Supabase控制台启用，不能由内容迁移代替。

## 历史边界

- 100表示事件—来源链接数，不是100部独立文献。
- 检索工具用于发现和核对书目，不充当史料或同行审阅者。
- 工匠接单流程、基层文书流程和虚构人物台词没有足够个案证据时，只能标为合理重建或叙事虚构。
- 当前公开发布只确认工程可运行、来源边界可见和状态提交受控。它不确认每项历史判断已经通过同行评议。

## 后续认证门禁

若未来恢复外部认证，必须：

1. 取得签名文件或可独立核验的机构审阅记录。
2. 补齐需要精确定位的版本、页码、卷次或稳定条目标识。
3. 对每个条目给出通过、降级、改写或删除结论。
4. 生成新的内容版本和发布迁移。
5. 重新运行内容、类型、单元、契约、隔离和浏览器回归测试。

在此之前，阶段11保持 `public-beta-unreviewed` 与 `provisional`，不得使用任何个人姓名、签章或机构名称制造背书。
