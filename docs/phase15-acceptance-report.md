# 阶段15验收报告

验收日期：2026-07-30

场景：`tang-changan-742`

内容版本：`11.0.0`

阶段状态：工程验收通过，尚未部署

历史审阅：`pending`

## 交付范围

| 项目 | 状态 | 证据 |
|---|---|---|
| 三行动日循环 | 已实现 | 第三次行动在原回合事务中推进至次日 06:00，并恢复精力；不增加模型调用或 `clientTurnId` |
| 七个系统入口 | 已实现 | 地图、属性、背包、关系、营生 / 公务、任务、家户均读取现有权威状态或显示真实空状态 |
| 高频状态常驻 | 已实现 | 底栏展示钱财、精力、健康、士气、声望和三个行动位 |
| 行动卷宗 | 已实现 | 展示叙事、3 个编辑选项、方向性影响、失败恢复和自由行动 |
| 阶层边界 | 已实现 | 商户与工匠归入平民生活网络；京兆文书归入胥吏生活网络；未开放品官或帝王系统 |
| 证据视图 | 已实现 | 复用 8 个证据地点及现有来源筛选，不宣称 108 坊精确复原 |
| 旧存档兼容 | 已实现 | `energy` 与 `morale` 由 Zod 默认值补齐，无需生产数据库迁移 |
| 响应式与键盘 | 已实现 | 1440 × 1000 与 390 × 844 无页面级横向溢出；快捷键在输入控件中停用 |
| 低动态 | 已实现 | 复用产品低动态状态；不新增粒子、炫光或重复闪光 |

## 对标边界

阶段15只吸收《皇帝成长计划》可抽象验证的交互机制：高频入口常驻、核心状态常驻、回合结算可读和一致面板组织。没有复制其美术、文本、地图或帝王系统。

当前数据不支持帝王玩法。项目也没有全国财政、人口、军事、官僚和宗室规则。用静态面板模拟这些系统会制造虚假完成，因此本阶段明确拒绝。

## 自动验证

`npm test` 于 2026-07-30 通过。总门禁包含：

- 跟踪文件密钥扫描通过；
- Supabase 迁移台账：30 个有序迁移通过；
- 内容校验：18 个来源、40 条声明、27 个事件、12 名 NPC、8 个地点；
- lint、TypeScript、生产构建和生产资源冒烟通过；
- 单元测试：21 个文件，411 项通过；
- 契约测试：44 项通过；
- Playwright：17 项通过，覆盖 axe、键盘、390 px、768 px、字体、离线状态和性能预算。

## 浏览器实测

使用已配置的 Supabase 测试项目建立一次匿名会话，并在真实游戏流程中检查：

- 1440 × 1000 与 390 × 844 两个断点；
- 七个系统入口与三个当前事件选项；
- `M`、`P`、`B`、`R`、`J` 和 `Space` 输入映射；
- 输入框聚焦时不劫持快捷键；
- 低动态开关；
- axe 扫描 0 项违规。

测试匿名用户及其数据已删除。

## 尚未完成

- 阶段15尚未部署到公开站点。
- 外部历史学者审阅仍为 `pending`。
- 27 条阶段11声明仍为 `provisional`，不能改称已审史实。
- 阶段14尚无足够真实参与者数据，不能宣称商业级完成。
- 品官、世族、宗室、帝王、婚姻、生育、跨代、全国地图与军事财政均未实现。

上述项目不是工程测试失败，而是需要独立内容、外部证据或新规则包的后续范围。
