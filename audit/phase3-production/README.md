# 阶段 3 生产站复核

## 范围

- 站点：`https://chronokalamos.burrows-bandeau-7x.chatgpt.site`
- 版本：Sites version 9
- 流程：登录站点、建立 Supabase 游客、写入幂等存档、刷新恢复、390px 与 768px 响应式复核
- 日期：2026-07-18

## 步骤与结论

1. `01-mobile-before.png`：390px 修复前。布局无横向溢出，但低动态、退出和三个时间轴按钮高度只有 12–20px。状态：不通过。
2. `02-mobile-after.png`：390px 修复后。地图、时间轴、身份卡和出身卡按单列落位；全部可见按钮不小于 24px。状态：通过。
3. `03-tablet-768.png`：768px 断点。主区域为单列，地图和身份卡完整，页面无横向溢出。状态：通过。
4. `04-desktop-signed-in.png`：1536×825。三列布局成立；游客身份可见；存档计数为 1；刷新后身份和存档仍保留。状态：通过。

## 数据证据

- 生产浏览器实际创建匿名用户与 1 个存档。
- 页面刷新后仍显示同一游客和 `OWN ARCHIVES / 1`。
- 复核后按完整 UUID、存档 UUID、匿名状态和标题限定条件清理测试用户。
- 清理后 `auth.users`、`profiles`、`game_sessions`、`user_uploads` 和私有桶对象计数均为 0。

## 证据边界

截图不能证明完整 WCAG 合规。本轮检查了响应式回流、横向溢出、按钮目标尺寸、可见身份状态和存档恢复。键盘全路径、对比度数值、读屏器输出、真实邮件投递和生产图片上传仍需独立验收。
