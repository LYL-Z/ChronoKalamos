# 742年长安候选章节

三条线均为九事件结构：

1. 一个开场。
2. 五个核心事件。
3. 两个读取既有选择的后果事件。
4. 一个章节收束。

`events.json` 是编辑层合同。`lib/game/phase11-catalog.ts` 将其编译为现有 `EventTemplate`，但所有条目保持 `provisional`。生产仓库仍只加载阶段10的 `published` 目录。

章节不等于九段史实。每个事件都必须结合 `classification`、`claimIds` 和 `evidenceRefs` 阅读。
